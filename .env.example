# StudyPilot AI — Environment Variables (EXAMPLE FILE — safe to commit)
#
# Copy this file to ".env" (no extension change needed, just remove ".example")
# and fill in real values there. The real ".env" file is git-ignored and will
# never be committed or pushed to GitHub.
#
# This variable is not needed until Day 6 (Claude API integration) — it's set
# up here as a placeholder so the project's configuration is complete today.

ANTHROPIC_API_KEY=your-api-key-goes-here-on-day-6
